import msalInstance from"./msal";
export async function getPbiToken() {
    const scopes=["https://analysis.windows.net/powerbi/api/.default"];
  letaccount =msal 'account' is assigned a value but never used. Allwed unsend variables must be used.

    };
    try {
        
    } catch (error) {
        
        
    }